ZAD BACKEND APPLICATION STRUCTURE
Audited: 2026-06-10

Purpose
This file documents the backend pieces that support the ZAD Hotels SSR website
and the xhotelpro Zad website editor.

The backend lives in:
D:\JannatBooking\hotels_backend

The frontend SSR website lives in:
D:\JannatBooking\zad_ssr

Runtime Mounting Rule
- D:\JannatBooking\hotels_backend\server.js dynamically loads route files from:
  D:\JannatBooking\hotels_backend\routes
- Every route file is mounted under /api.
- Therefore D:\JannatBooking\hotels_backend\routes\zad.js exposes endpoints under /api.

Main Backend Files
- D:\JannatBooking\hotels_backend\models\zad_website.js
  - Mongoose model for the Zad website document.
  - Uses siteKey="zad".
  - Stores content fields compatible with the existing Jannat website upload widgets:
    - janatLogo
    - homeMainBanners
    - homeSecondBanner
    - homeThirdBanner
    - contactUsBanner
    - aboutUsBanner
    - aboutUsPhoto
    - hotelPageBanner
    - footerBanner
    - aboutUsEnglish
    - aboutUsArabic
    - terms/privacy fields
    - contactEmail
    - officialEmail
    - phone
    - whatsappNumber
    - brandPalette
  - Default logo:
    https://res.cloudinary.com/infiniteapps/image/upload/v1781132268/zad/defaults/logo.png
  - Default carousel:
    https://res.cloudinary.com/infiniteapps/image/upload/v1781132268/zad/defaults/home-carousel-1.jpg
    https://res.cloudinary.com/infiniteapps/image/upload/v1781132269/zad/defaults/home-carousel-2.jpg
    https://res.cloudinary.com/infiniteapps/image/upload/v1781132270/zad/defaults/home-carousel-3.jpg
  - Default footer background:
    https://res.cloudinary.com/infiniteapps/image/upload/v1781132271/zad/defaults/footer-pattern.jpg
  - Default Arabic About content is populated so Arabic visitors do not see a blank About page.

- D:\JannatBooking\hotels_backend\controllers\zadcontroller.js
  - Main Zad public website controller.
  - Main manager/editor controller for the Zad website document.
  - Owner email constant:
    process.env.ZAD_OWNER_EMAIL || "mrgamal@xhoteltest.com"
  - Important behavior:
    - Public hotel endpoints are fenced to hotels connected to the Zad owner email.
    - Public hotel responses strip private agent room override data.
    - Website document updates are allowed only to the Zad website manager email or configured platform super admin.
    - Public list endpoints use short in-memory cache headers and compact response shapes.
    - Website document responses merge footerBanner and Arabic defaults when missing.

- D:\JannatBooking\hotels_backend\routes\zad.js
  - Express route file.
  - Loaded automatically by server.js under /api.
  - Uses requireSignin/isAuth for saving website document.
  - Uses userById route param for authenticated editor saves.

Public API Endpoints
All endpoints below are mounted under /api.

- GET /api/zad-website-document
  - Controller:
    listZadWebsiteDocuments
  - Used by:
    D:\JannatBooking\zad_ssr\lib\api.js
    D:\JannatBooking\hotels_frontend\src\HotelModule\apiAdmin.js
  - Returns the saved Zad website document or defaults if none exists.

- GET /api/zad/scope-health
  - Controller:
    getZadScopeHealth
  - Purpose:
    Quick diagnostic for owner email resolution and hotel scope count.

- GET /api/zad/active-hotels
  - Controller:
    listOfAllActiveZadHotels
  - Used by the home page featured hotels.

- GET /api/zad/active-hotel-list
  - Controller:
    getZadListOfHotels
  - Used by:
    hotel listing
    support widget hotel choices
    search/filter pages

- GET /api/zad/distinct-rooms
  - Controller:
    distinctZadRoomTypes
  - Used by the room search form.

- GET /api/zad/single-hotel/:hotelSlug
  - Controller:
    getZadHotelFromSlug
  - Used by:
    D:\JannatBooking\zad_ssr\app\single-hotel\[slug]\page.js

- GET /api/zad/room-query-list/:query
  - Controller:
    gettingZadRoomListFromQuery
  - Query format:
    startDate_endDate_roomType_adults_children_destination
  - Used by:
    D:\JannatBooking\zad_ssr\app\rooms\page.js

- GET /api/zad/hotels/active-with-deals
  - Controller:
    listOfAllActiveZadHotelsMonthlyAndOffers
  - Used by:
    D:\JannatBooking\zad_ssr\app\zad-offers-monthly-reservations\page.js

Authenticated Website Editor Endpoint
- POST /api/zad-website/:documentId/:userId
  - Controller:
    createUpdateZadWebsiteDocument
  - Guards:
    requireSignin
    isAuth
  - User param:
    router.param("userId", userById)
  - Used by:
    D:\JannatBooking\hotels_frontend\src\HotelModule\apiAdmin.js
    D:\JannatBooking\hotels_frontend\src\HotelModule\TheOverallStructure\ZadWebsite\ZadWebsiteManager.js
  - Authorization:
    Only mrgamal@xhoteltest.com or configured platform super admin can update the Zad website document.

Hotel Scope Logic
- Zad owner email:
  mrgamal@xhoteltest.com by default.
- Controller resolves the User document by email.
- Controller collects hotel ids from:
  - hotelIdWork
  - hotelIdsWork
  - hotelsToSupport
  - hotelIdsOwner
  - HotelDetails documents where belongsTo is the owner id or belongsToId
- Public hotel filter requires:
  - _id in resolved Zad hotel ids
  - activateHotel: true
  - xHotelProActive not false
  - hotel photos
  - valid location coordinates
  - active public rooms with base price and room photos for most list endpoints

Related Existing Backend Models
- D:\JannatBooking\hotels_backend\models\user.js
  - Used to resolve mrgamal@xhoteltest.com and hotel assignments.
- D:\JannatBooking\hotels_backend\models\hotel_details.js
  - Used to load public hotel, room, photo, location, distance, offers, and monthly data.
- D:\JannatBooking\hotels_backend\models\supportcase.js
  - Used by the existing support-case APIs.
  - The Zad SSR support widget creates normal hotel-scoped support cases through existing supportcase routes.

Related Existing Backend Support Routes
- D:\JannatBooking\hotels_backend\routes\supportcase.js
  - POST /api/support-cases/new
  - GET /api/support-cases/client/:id
  - PUT /api/support-cases/client/:id
- D:\JannatBooking\hotels_backend\controllers\supportcase.js
  - createNewSupportCase
  - getPublicClientSupportCaseById
  - updatePublicClientSupportCase

Important Support Widget Rule
- The Zad SSR support widget must not use the old Jannat/global support hotel id.
- It requires a real Zad hotel selection from /api/zad/active-hotel-list.
- It sends supportScope="hotel".
- It sends hotelId and ownerId from the selected Zad hotel.

Related XHotelPro Frontend API Files
- D:\JannatBooking\hotels_frontend\src\HotelModule\apiAdmin.js
  - getZadWebsiteRecord()
  - saveZadWebsiteRecord(documentId, userId, payload)

Related XHotelPro Editor/UI Files
- D:\JannatBooking\hotels_frontend\src\HotelModule\TheOverallStructure\ZadWebsite\ZadWebsiteManager.js
  - Zad website editor page.
- D:\JannatBooking\hotels_frontend\src\HotelModule\AdminOverallSideMenu\AdminOverallSideMenu.js
  - Adds side-menu entry only for mrgamal@xhoteltest.com.
- D:\JannatBooking\hotels_frontend\src\HotelModule\TheOverallStructure\page.js
  - Maps overall=zad-website to ZadWebsiteManager.

Maintenance Notes
- Keep all Zad public hotel reads under routes/zad.js and controllers/zadcontroller.js.
- Do not reuse public Janat endpoints for zadhotels.com.
- Do not expose private agentInventory or agentPricingRate data in public Zad responses.
- Do not loosen the website editor authorization without business approval.
- If the Zad owner email changes, prefer setting ZAD_OWNER_EMAIL in environment rather than hardcoding a new email.
- If the default Zad Cloudinary assets change, update both:
  - D:\JannatBooking\hotels_backend\controllers\zadcontroller.js
  - D:\JannatBooking\hotels_backend\models\zad_website.js
- Existing saved Zad website documents may keep old custom image URLs. Replace only old default Zyro URLs in production data, and do not overwrite user-uploaded custom images.
- After backend changes, run:
  - node --check D:\JannatBooking\hotels_backend\controllers\zadcontroller.js
  - node --check D:\JannatBooking\hotels_backend\routes\zad.js
  - node --check D:\JannatBooking\hotels_backend\models\zad_website.js

Verification Performed
- node --check passed for:
  - D:\JannatBooking\hotels_backend\controllers\zadcontroller.js
  - D:\JannatBooking\hotels_backend\routes\zad.js
  - D:\JannatBooking\hotels_backend\models\zad_website.js
- D:\JannatBooking\hotels_frontend npm run build passed after the Zad editor/menu changes.
- D:\JannatBooking\zad_ssr npm run build passed against the Zad API helper fallback behavior.
- 2026-06-10 Cloudinary/default asset pass:
  - node --check passed for:
    - D:\JannatBooking\hotels_backend\controllers\zadcontroller.js
    - D:\JannatBooking\hotels_backend\models\zad_website.js
  - Zad SSR npm run build passed after reading the Cloudinary defaults and footerBanner field.

Zad Cloudinary Defaults And Footer Field - 2026-06-10
- Uploaded the provided Zad default assets to Cloudinary and made those URLs the application defaults.
- Added footerBanner to the Zad website document model and controller sanitizer.
- Added merge fallback for footerBanner so old documents still receive a footer image if they do not have one.
- Added Arabic About default content.
- Corrected the third default carousel button route to /rooms.
- Production data note:
  - The live Zad website document may already exist in MongoDB with old Zyro default URLs.
  - During deployment, update only the old default Zyro URLs to the new Cloudinary URLs.
  - Do not replace any hotel group custom uploads that are not the old default Zyro URLs.

Production Deployment Plan - 2026-06-10
This section documents the intended backend and production rollout steps before execution.

1. Push backend code
   - Commit and push:
     - D:\JannatBooking\hotels_backend\controllers\zadcontroller.js
     - D:\JannatBooking\hotels_backend\models\zad_website.js
     - D:\JannatBooking\hotels_backend\routes\zad.js
   - These files are required before the public Zad SSR website can read real Zad hotel data.

2. Push xhotelpro frontend code
   - Commit and push the Zad website editor/menu integration in hotels_frontend.
   - This gives mrgamal@xhoteltest.com access to the new ZAD Website editor link.

3. Push Zad SSR code
   - Initialize and push D:\JannatBooking\zad_ssr to:
     https://github.com/AhmedAbdelRazak/zad_ssr.git
   - This repository contains the public Next.js website.

4. Pull on home server
   - SSH command:
     ssh jannat
   - Server directory:
     /home/ahmedadmin/Hotels
   - Pull or clone:
     - hotels_backend
     - hotels_frontend
     - zad_ssr

5. Production backend health
   - Run node --check for the Zad controller/model/routes on the server.
   - Restart hotels-backend with PM2.
   - Confirm PM2 shows hotels-backend online.
   - Confirm /api/zad/scope-health returns a valid response.

6. Subdomain and proxy
   - Target hostname:
     zadhotels.jannatbooking.com
   - Target local service:
     zad_ssr Next.js app on port 3105.
   - If web-server changes require sudo, document the exact command or final config used.

Production Deployment Result - 2026-06-10
- GitHub push completed.
  - hotels_backend branch master:
    commit 99f6c70 - Add Zad public website API
  - hotels_frontend branch master:
    commit 2693f87 - Add Zad website editor
  - zad_ssr branch main:
    commit 7002403 - Initial Zad SSR website

- Backend production deployment completed on ssh jannat.
  - Server path:
    /home/ahmedadmin/Hotels/hotels_backend
  - The server fast-forwarded from cf6f019 to 99f6c70.
  - These files were added on production:
    controllers/zadcontroller.js
    models/zad_website.js
    routes/zad.js
  - Production syntax checks passed:
    node --check controllers/zadcontroller.js
    node --check routes/zad.js
    node --check models/zad_website.js
  - PM2 service hotels-backend was restarted and is online.

- Backend production health check completed.
  - Checked locally on the server:
    http://127.0.0.1:8080/api/zad/scope-health
  - Checked through the public production API:
    https://xhotelpro.com/api/zad/scope-health
  - Both returned HTTP 200.
  - The response confirmed:
    ownerEmail: mrgamal@xhoteltest.com
    ownerFound: true
    hotelCount: 7

- XHotelPro production deployment completed.
  - Server path:
    /home/ahmedadmin/Hotels/hotels_frontend
  - The server fast-forwarded from 5893eeb to 2693f87.
  - npm run build completed successfully.
  - PM2 service hotels-frontend was restarted and is online.

- Zad SSR production deployment completed.
  - Server path:
    /home/ahmedadmin/Hotels/zad_ssr
  - Repository:
    https://github.com/AhmedAbdelRazak/zad_ssr.git
  - Branch and commit:
    main at 7002403
  - Server runtime:
    Node v24.15.0
    npm 11.12.1
  - npm ci completed with 0 vulnerabilities.
  - npm run build completed successfully.
  - PM2 service zad-ssr was started and is online on port 3105.
  - PM2 process list was saved with pm2 save.

- Zad SSR smoke checks completed on the server.
  - http://127.0.0.1:3105/ returned HTTP 200.
  - http://127.0.0.1:3105/our-hotels returned HTTP 200.
  - http://127.0.0.1:3105/rooms returned HTTP 200.

- Subdomain status and remaining production step.
  - Requested hostname:
    zadhotels.jannatbooking.com
  - DNS lookup result:
    The hostname does not exist yet.
  - Home server public IP:
    107.213.56.128
  - Nginx is active on the server, but adding a new Nginx site requires sudo.
  - The current SSH user could not run sudo without a password during deployment.

- Required finish steps for the public URL.
  1. In DNS for jannatbooking.com, add:
     Type: A
     Name: zadhotels
     Value: 107.213.56.128
  2. On ssh jannat, add an Nginx site that sends zadhotels.jannatbooking.com to:
     http://127.0.0.1:3105
  3. Test Nginx, reload it, then run certbot for SSL once DNS resolves.

- Recommended Nginx site content for the sudo step.
  server {
      listen 80;
      server_name zadhotels.jannatbooking.com;

      client_max_body_size 100M;

      location / {
          proxy_pass http://127.0.0.1:3105;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection "upgrade";
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto https;
          proxy_cache_bypass $http_upgrade;
      }
  }

- Suggested sudo commands after DNS resolves to 107.213.56.128.
  sudo tee /etc/nginx/sites-available/zadhotels > /dev/null <<'EOF'
  server {
      listen 80;
      server_name zadhotels.jannatbooking.com;

      client_max_body_size 100M;

      location / {
          proxy_pass http://127.0.0.1:3105;
          proxy_http_version 1.1;
          proxy_set_header Upgrade $http_upgrade;
          proxy_set_header Connection "upgrade";
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto https;
          proxy_cache_bypass $http_upgrade;
      }
  }
  EOF
  sudo ln -sfn /etc/nginx/sites-available/zadhotels /etc/nginx/sites-enabled/zadhotels
  sudo nginx -t
  sudo systemctl reload nginx
  sudo certbot --nginx -d zadhotels.jannatbooking.com

Cloudflare Tunnel Recovery And Final Public Routing - 2026-06-10
- The production public routing now uses Cloudflare Tunnel, not a direct DNS A record to the home server.
- Tunnel name:
  jannat-home-tunnel
- Tunnel ID:
  c5ffbb23-3451-4d5e-9721-d1f5e9975285
- Home server service:
  cloudflared.service
- Tunnel credentials/config are on the home server under:
  /etc/cloudflared
- Cloudflare sends the public hostnames to the tunnel origin:
  http://127.0.0.1:80
- Nginx then routes by Host header:
  - jannatbooking.com -> existing Jannat frontend
  - www.jannatbooking.com -> existing Jannat frontend
  - zadhotels.jannatbooking.com -> Zad SSR app on http://127.0.0.1:3105
- Cloudflare tunnel routes restored/created:
  - jannatbooking.com
  - www.jannatbooking.com
  - zadhotels.jannatbooking.com
- Verification from ssh jannat returned HTTP/2 200 for:
  - https://jannatbooking.com
  - https://www.jannatbooking.com
  - https://zadhotels.jannatbooking.com
- Important:
  - Do not run certbot for these Cloudflare Tunnel hostnames.
  - Cloudflare handles public HTTPS for the tunnel.
  - The old direct A-record/certbot instructions above are only a fallback if the Cloudflare Tunnel is intentionally removed later.
- The correct tunnel validation command is:
  sudo cloudflared --config /etc/cloudflared/config.yml tunnel ingress validate
- aivideo.jannatbooking.com and agentai.jannatbooking.com are not needed for this setup. If they still appear in Cloudflare DNS, remove those two Tunnel records from the Cloudflare dashboard.

Production Zad Defaults Deployment Result - 2026-06-10
- GitHub push completed for the backend Zad default asset update:
  - Repository:
    https://github.com/AhmedAbdelRazak/hotels_backend.git
  - Branch:
    master
  - Commit:
    d1880b0 - Update Zad Cloudinary defaults
- Production server pull completed on ssh jannat:
  - Path:
    /home/ahmedadmin/Hotels/hotels_backend
  - The server fast-forwarded from 99f6c70 to d1880b0.
- Production syntax checks passed:
  - node --check controllers/zadcontroller.js
  - node --check models/zad_website.js
- PM2 production restart completed:
  - hotels-backend restarted and is online.
- Mongo safety update was run for the Zad website document:
  - Document id:
    6a29f440bf84f4bb283df3bd
  - It only targets old default Zyro asset URLs and missing footer/Arabic fields.
  - Result:
    no data changes were needed because the document already had the expected Cloudinary/default values.
- Public API verification:
  - https://xhotelpro.com/api/zad-website-document returned:
    - Cloudinary logo URL
    - Cloudinary first carousel URL
    - Cloudinary footer URL
    - aboutUsArabic present
    - third carousel route set to /rooms

Zad Checkout, PayPal, And Support Origin Backend Notes - 2026-06-10
- D:\JannatBooking\hotels_backend\models\supportcase.js now stores public support origin fields:
  - sourceWebsite
  - sourcePage
  - sourceUrl
- D:\JannatBooking\hotels_backend\controllers\supportcase.js accepts those fields when creating a support case.
  - Zad sends sourceWebsite="zad_ssr".
  - This lets the same supportcase collection and Socket.IO flow identify cases opened from zadhotels.
  - The case is still supportScope="hotel" and still uses the selected Zad hotel/owner.
- D:\JannatBooking\hotels_backend\controllers\paypal_reservation.js now returns the public PayPal clientId with /api/paypal/token-generated.
  - This is needed because zad_ssr is a Next.js app and should not need local hardcoded PayPal public client ids to initialize the PayPal buttons.
  - The PayPal secret remains server-side only.
- D:\JannatBooking\hotels_backend\controllers\zadcontroller.js now includes guestPaymentAcceptance in public Zad hotel payloads.
  - Zad checkout uses that hotel setting to decide which payment options are visible:
    - deposit online
    - full online payment
    - reserve now, pay in hotel
- Related Zad SSR frontend files:
  - D:\JannatBooking\zad_ssr\components\CheckoutClient.js
  - D:\JannatBooking\zad_ssr\lib\booking.js
  - D:\JannatBooking\zad_ssr\components\SupportWidget.js
- Verification:
  - node --check passed for:
    - controllers/supportcase.js
    - controllers/paypal_reservation.js
    - controllers/zadcontroller.js
    - models/supportcase.js

Zad Asset Delivery And Image Protection Notes - 2026-06-10
- The home-server asset documentation used for this pass:
  - D:\HomeServer\Ahmed_Home_Server_Cloudinary_Local_Assets_Automation_Documentation.docx
- Relevant home-server asset behavior from that document:
  - Static files live under:
    /var/www/jannat-assets
  - Public apps already use /assets/... as the stable home-server asset path.
  - Existing Nginx local asset serving uses long cache headers and no directory listing.
  - Cloudinary fallback metadata is preserved in MongoDB and should not be blindly deleted.
- Zad SSR now has a same-origin /assets/[...path] route in the frontend app.
  - This keeps displayed hotel images on a Zad URL path while still reading the existing home-server /assets files.
  - Old database image URLs that point at https://jannatbooking.com/assets/... are normalized in the frontend to /assets/....
  - No backend database migration was required for this PageSpeed pass.
- Security behavior:
  - /assets and /_next/image responses include noindex/noimageindex and same-origin resource policy headers.
  - Zad proxy blocks normal cross-site hotlink attempts for /assets and /_next/image when the public host is zadhotels.jannatbooking.com.
  - This reduces image reuse and hotlinking, but it cannot prevent screenshots or determined browser downloads.
- Backend impact:
  - hotels_backend code was not changed for this pass.
  - Existing hotel image URLs and Cloudinary fallback fields remain untouched.

Production Result - Zad Asset Delivery And Image Protection - 2026-06-10
- Runtime change deployed through zad_ssr, not hotels_backend.
- Production server:
  - /home/ahmedadmin/Hotels/zad_ssr is on commit 60d6c75.
  - npm ci and npm run build passed on ssh jannat.
  - pm2 restarted zad-ssr and saved the process list.
- Live checks:
  - Zad public pages returned 200.
  - Zad /assets route returned 200 with long-cache and noimageindex headers.
  - Zad /_next/image optimized route returned 200 for a hotel image.
  - Cross-site image hotlink simulation returned 403.
- No hotels_backend deployment was required for this item.
